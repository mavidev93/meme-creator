//react
import { createSlice } from "@reduxjs/toolkit";

interface Text{ 
    value: string;
    color: string;
    position: string;
    font: string;
    id: string;

}

initialState:

export const memeTextsSlice = createSlice({

})